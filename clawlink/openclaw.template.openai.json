{
  // ClawLink Brain Node — OpenAI GPT 연동 설정 템플릿
  // 브랜치: llm/openai
  agents: {
    defaults: {
      model: {
        primary: "openai/gpt-4o",
        fallbacks: ["openai/gpt-4o-mini"]
      }
    }
  },
  env: {
    OPENAI_API_KEY: "${OPENAI_API_KEY}"
  },
  gateway: {
    port: 18789
  }
}
